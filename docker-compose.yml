services:
  postiz-application.application:
    container_name: postiz-application.application
    image: webgrip/postiz-application:latest
    pull_policy: never
    build:
      context: .
      dockerfile: ops/docker/application/Dockerfile
    restart: always
    depends_on:
      postiz-application.mariadb:
        condition: service_healthy
      postiz-application.redis:
        condition: service_healthy
    ports:
      - "127.0.0.1:9000:9000"
    env_file: ./.env
    environment:
      DB_TYPE: ${DB_CONNECTION:-}
      DB_STRICT: "false"
      LOG_CHANNEL: stack
      REQUIRE_HTTPS: "false"
    volumes:
      - ./.env:/var/www/app/.env
      - postiz-application-application-public-data:/var/www/app/public
      - postiz-application-application-storage-data:/var/www/app/storage

  postiz-application.nginx:
    container_name: postiz-application.nginx
    image: webgrip/postiz-application-nginx:latest
    pull_policy: never
    build:
      context: .
      dockerfile: ops/docker/nginx/Dockerfile
    restart: always
    depends_on:
      - postiz-application.application
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - postiz-application-application-public-data:/var/www/app/public:ro

  postiz-application.mariadb:
    container_name: postiz-application.mariadb
    image: webgrip/postiz-application-mariadb:latest
    build:
      context: .
      dockerfile: ops/docker/mariadb/Dockerfile
    pull_policy: never
    restart: unless-stopped
    ports:
      - "127.0.0.1:3306:3306"
    logging:
      options:
        max-size: 10m
    env_file:
      - ./.env
    environment:
      MARIADB_DATABASE: ${DB_DATABASE:-application}
      MARIADB_USER: ${DB_USERNAME:-application}
      MARIADB_PASSWORD: ${DB_PASSWORD:-application}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root}
      MARIADB_CHARACTER_SET_SERVER: utf8mb4
      MARIADB_COLLATION_SERVER: utf8mb4_unicode_ci
    volumes:
      - postiz-application-mariadb-data:/var/lib/mysql
    healthcheck:
      test:
        [
          "CMD",
          "mariadb",
          "--user=${DB_USERNAME:-application}",
          "--password=${DB_PASSWORD:-application}",
          "--execute=SELECT 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  postiz-application.redis:
    container_name: postiz-application.redis
    image: webgrip/postiz-application-redis:latest
    pull_policy: never
    build:
      context: .
      dockerfile: ops/docker/redis/Dockerfile
    restart: always
    volumes:
      - postiz-application-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 5s

volumes:
  postiz-application-application-storage-data:
  postiz-application-application-public-data:

  postiz-application-mariadb-data:
  postiz-application-redis-data:
  certificate-data:
    external: true

networks:
  default:
    external: true
    name: webgrip
